<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bd_sqlite_room">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BD_SQLite_ROOM">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityMenu"></activity>
        <activity android:name=".AtcivityAltas"></activity>
        <activity android:name=".ActivityConsultas"></activity>
        <activity android:name=".ActivityModificaciones"></activity>
        <activity android:name=".ActivityBajas"></activity>

    </application>

</manifest>